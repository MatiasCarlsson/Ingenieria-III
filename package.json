{
  "name": "yvy-pora-ingenieria-iii",
  "version": "1.0.0",
  "private": true,
  "description": "Proyecto de huerta urbana - Actividad #8 CI",
  "scripts": {
    "lint": "eslint JS/*.js",
    "test": "jest --ci --coverage --coverageReporters=text --coverageReporters=lcov",
    "build": "node -e \"console.log('Build OK: static app')\""
  },
  "devDependencies": {
    "eslint": "^9.25.1",
    "globals": "^15.15.0",
    "jest": "^29.7.0"
  }
}
